    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ entity.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Description2</th>
                <td>{{ entity.description2 }}</td>
            </tr>
        </tbody>
    </table>


    <div class="btn-group" role="group">


        <a href="{{ path('product_edit', { 'id': entity.id }) }}" id="vedit_id_{{ entity.id }}" class="edit btn btn-default">
            Edit
        </a>

        {{ form(delete_form) }}

    </div>

